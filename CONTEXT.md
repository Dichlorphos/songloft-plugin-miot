# 智能音箱播放同步上下文

## 领域术语

- **设备**：由米家账号管理、可通过 MIoT 控制的音箱。
- **播放管理器**：与一个独立设备或设备组绑定的 PlaylistManager，持有歌单、歌曲索引、播放模式和切歌定时器。
- **最新播放快照**：按账号保存的本插件最近一次成功播放歌曲及其曲内进度、倍速、播放模式和来源设备。
- **播放上下文同步**：切换独立目标设备时，把最新播放快照加载到目标 PlaylistManager 的内存上下文，不向音箱发出播放命令。
- **继续播放**：用户通过现有播放控制或语音命令，让目标 PlaylistManager 按已同步上下文重新生成 URL 并起播。

## 边界

本功能只处理未加入设备组的独立设备。设备组继续由 GroupCoordinator 和共享 PlaylistManager 负责，保持现有行为。

最新播放快照按账号隔离，仅记录本插件成功播放的正式歌单歌曲；临时歌单和外部播放不参与同步。目标设备成为当前设备后异步加载未过期快照；没有快照或加载失败时保留目标原上下文。

切换不自动播放、不暂停、不停止目标设备。用户继续播放时恢复歌曲、位置、倍速和播放模式。快照有效期为 30 分钟，设备离线、unknown、超时或存储失败不清除既有记录。

普通语音和 AI 都区分 pause、stop、resume：pause 保留位置并更新快照，stop 保持停止语义，resume 使用目标 PlaylistManager 当前上下文。旧 AI 返回 stop 时按停止处理。

## 实现约束补充

- 设备切换创建或取得目标 PlaylistManager 时，不触发热重载专用的 resumeAfterReload 自动起播；插件启动或热重载仍保留原有恢复行为。
- 播放上下文加载与真正起播必须分离：上下文加载只加载歌单、定位歌曲、设置位置/倍速/播放模式和 stopped/paused 状态，不推送 URL、不启动自动切歌定时器；现有播放/继续操作负责真正起播。

## 活动播放设备边界

目标设备正在播放时，切换只保存待播放上下文，不覆盖目标 PlaylistManager 当前活动上下文，也不向音箱发命令。重复切换到同一目标时，对未变化的待播放上下文去重。用户点击现有播放/继续控制后，才消费待播放上下文、覆盖目标 PlaylistManager 并起播；播放成功后清除待播放上下文。目标处于任何状态时都只保存待播放上下文，仍不自动播放。

