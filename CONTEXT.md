# 智能音箱播放同步上下文

本文定义需求中的领域术语，不表示功能已经实现。需求规则和验收标准以[完整需求规范](.scratch/playback-sync/spec.md)为准。

## 领域术语

- **设备**：由米家账号管理、可控制播放的音箱。
- **独立设备**：未加入设备组的设备。
- **设备组**：米家账号配置为共同播放目标的一组设备，共享一个播放上下文。
- **源设备**：切换请求开始前用户当前选中的设备，提供最新播放快照所记录的播放内容和状态；跨账号切换不共享其快照。
- **目标设备**：在一次播放上下文同步中，作为待播放上下文接收方的独立设备。
- **内容类型**：快照记录的媒体来源类别，分为正式歌单歌曲和电台。
- **最新播放快照**：按账号记录的最近一次符合范围要求的成功播放上下文，包含内容类型、歌曲身份、歌单或电台身份、索引、曲内位置及其可用性、倍速、播放模式、状态和源设备。
- **快照记录点**：`PlaylistManager` 状态机中写出最新播放快照的出口；网页、普通语音、AI、定时任务、睡眠定时器和内部自动停止都经同一组出口，因此不需要按调用入口逐个埋点。
- **播放快照存储**：按账号保存最新播放快照的观测结果，与待播放上下文分开存放。
- **待播放上下文存储**：按账号与目标设备保存待播放上下文的决策产物，与播放快照分开存放。
- **活动播放上下文**：目标设备当前使用的歌单、歌曲和播放状态。
- **待播放上下文**：切换到目标设备时为其保存、等待用户明确继续后使用的播放快照，与活动播放上下文分离。`pending` 是该概念在代码与存储中的既有命名（如 `PendingContext`、`playback_pending_v1`、`pending` 字段），属于同一概念的别名，不表示另一个领域对象。
- **播放上下文同步**：将账号级最新播放快照保存为独立目标设备的待播放上下文。
- **继续播放**：用户明确要求恢复播放的操作；有有效待播放上下文时使用该上下文，否则使用目标原上下文。
- **播放状态**：播放上下文当前所处的状态，包括 `playing`、`paused` 和 `stopped`。
- **同步结果**：一次播放上下文准备或下发的结果，分为 `succeeded`、`failed` 和 `unknown`；`unknown` 表示设备可能已收到请求但无法确认最终状态。
- **起播确认**：播放请求下发成功后，延迟回读设备真实播放状态以确认设备确实拉到了流的校验；未通过时按起播失败处理，避免「云端受理成功但设备没播」被当成正常播放。
- **起播失败**：一次播放请求未能确认设备进入播放的状态；可跳歌的内容（正式歌单）跳到下一首，无可跳内容（电台、单曲播放）停播，连续多次失败触发熔断。
- **停止位置**：一次停止发生时记录的曲内位置；用户明确继续后从该位置继续播放，而不是从头重播。它只对同一首歌有效——换歌或换歌单后即失效，且位置不可用时退化为从头播。
- **停止后恢复**：`stopped` 状态下用户明确继续时，以停止位置带 `seek` 重推播放流，使听感接在断点处；与**暂停后恢复**的区别是后者复用设备端现成的媒体上下文（除非设备把暂停升级为停止）。
- **重启恢复**：插件启动/重启后按持久化锚点还原播放状态。用**硬条件**判定而不看锚点年龄：`playing` 只有「设备此刻确实还在放我们这条流」才接管自动切歌定时器；拿不到该硬证据（设备没在放、在放别的媒体、流长对不上、状态查不到）一律钉死为 `stopped`，任何情况下都不重推 URL 把音箱叫起来。`paused` / `stopped` 只还原本地状态与位置，不向设备下发指令。
- **清洁停机标记**：`onDeinit` 写下、`onInit` 读取后立即清除的一次性存储标记。上次启动没读到它，说明进程被崩溃/强杀/断电带走（异常终止），重启时无论设备状态如何都不自动接管续播。
