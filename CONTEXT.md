# 智能音箱播放同步上下文

## 领域术语

- **设备**：由米家账号管理、可通过 MIoT 控制的音箱。
- **播放管理器**：与一个独立设备或设备组绑定的 PlaylistManager，持有歌单、歌曲索引、播放模式和切歌定时器。
- **播放状态**：设备的 playing、paused、stopped 或 unknown，以及曲内 position、duration 和当前歌曲身份。
- **最近播放快照**：持久化的最近一次有效播放状态，用于设备切换后的续播。
- **源设备**：同步任务中提供歌曲和播放状态的设备。
- **目标设备**：同步任务中接收播放或暂停控制的设备。
- **续播**：在目标设备上恢复同一歌单歌曲，并从快照记录的曲内位置开始。
- **实时镜像**：按固定低频间隔把源设备的歌曲、暂停状态和明显进度变化反映到目标设备。

## 边界

设备组已经由 GroupCoordinator 负责统一播放控制；播放同步功能面向不同设备之间的切换和镜像。设备状态优先取 resolvePlayerStatus 的融合结果，避免绕过现有 seek 偏移、倍速和缓存逻辑。
# 切换设备播放上下文同步

## 领域术语

- **最新播放快照**：按账号保存的本插件最近一次成功播放歌曲及其曲内进度、倍速和来源设备。
- **播放上下文同步**：切换目标设备时，将最新播放快照加载到目标播放管理器，但不向音箱发出播放命令。
- **继续播放**：用户通过现有播放控制触发目标设备依据已同步上下文重新生成 URL 并起播。

## 边界补充

- 本次跨设备播放上下文同步仅适用于未加入设备组的独立设备。
- 目标设备属于设备组时保持现有行为，不执行跨设备快照同步。
- 设备组继续由 GroupCoordinator 和共享 PlaylistManager 独立负责。

## 已确认的播放上下文规则

- 目标设备成为当前设备后立即同步上下文。
- 账号级最新快照只在插件播放、暂停、切歌成功后更新。
- 同步字段包括歌曲、歌单、歌曲索引、曲内位置、倍速和播放模式。
- 同步覆盖目标 PlaylistManager 的内存上下文，但不向音箱发送播放命令。
- 临时歌单不参与跨设备同步。
- 用户点击现有继续按钮后，按快照歌曲、位置、倍速和播放模式直接起播。
- 快照过期后回退到目标设备自己的歌单进度。
- 歌曲失效按索引回退，索引失效则从歌单首曲开始；歌单不存在时保持目标设备现状并返回错误。
- 离线目标设备仍可切换并保留待同步上下文，实际播放时报告设备错误。

## 语音控制边界

- 普通语音和 AI 语音都区分 `pause` 与 `stop`。
- “暂停”调用 PlaylistManager.pause()，保留曲内位置并更新最新播放快照。
- “停止/关闭音乐”调用 PlaylistManager.stop()，保持现有停止语义。
- “继续播放”复用目标设备当前 PlaylistManager 上下文，包含切换时注入的独立设备播放上下文。
- 旧版本或异常 AI 返回 `stop` 时继续按停止处理，保证兼容。
- 设备组中的语音暂停、停止和继续继续由现有共享 PlaylistManager 与 GroupCoordinator 处理。
