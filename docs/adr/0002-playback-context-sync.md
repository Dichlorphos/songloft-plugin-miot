# ADR-0002：切换设备使用待播放上下文

- 状态：提议
- 日期：2026-09-12

以账号级最新播放快照作为独立目标设备的待播放上下文。切换只保存上下文，不自动播放、不覆盖目标活动状态；用户明确继续且准备成功后才消费并起播。设备组沿用共享 PlaylistManager。

该取舍解决了设备活动状态保护、重复切换抖动和异步失败回退问题；完整行为、数据契约和验收标准见 [spec.md](../../.scratch/playback-sync/spec.md)。
