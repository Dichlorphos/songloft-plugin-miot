# 音响切换同步需求

Status: ready-for-agent

## 代码基线

当前处于需求整理阶段；试做已还原，本需求相对于试做前没有业务代码改动。`11bd80b` 是试做前基线；`94ce724`、`8b01630` 是历史试做；`ab3a53f` 还原试做。当前 HEAD 与试做前基线的差异仅限需求 Markdown 文档。

当前源码不存在 `PlaybackSnapshot`、`PlaybackSyncService`、`PlaybackResumeService` 或最近播放接口。既有播放管理器、设备控制、设备级进度和热重载恢复属于原有能力。

## 目标

切换到未加入设备组的独立目标设备时，异步保存账号级最新播放快照为该设备的待播放上下文。复用现有播放、继续和设备选择入口，不新增最近播放入口或切换 API。

## 范围和规则

- 快照只来自本插件成功播放的正式歌单歌曲；临时歌单、外部 URL、单曲直推和外部播放不参与。
- 符合范围的播放至少一台目标设备成功后写入快照；快照存储失败不影响核心播放控制。
- 快照按账号保存一条；不同独立目标设备分别保留待播放上下文，严格按 `account_id` 隔离。
- 快照包含账号、`song_id`、`playlist_id`、索引、位置、倍速、播放模式、状态、来源设备、更新时间、诊断标题/歌手和整数 `schema_version`。
- `playing` 在切换请求开始时采样源设备物理位置；采样失败保留旧快照。恢复使用固定采样位置，不额外外推，误差相对采样时刻不超过 5 秒。
- pause 保存暂停位置；stop 保存停止前位置并标记 `stopped`。stopped 不自动起播，用户明确继续后仍可恢复。
- 快照和各目标待播放上下文跨插件重启保留，有效期为 30 分钟；无快照、过期、无效 schema 或同步保存失败时保留目标原上下文。源设备离线、unknown、超时或存储失败不清除有效记录，仍可使用未过期快照及其固定位置。
- 切换仅写待播放上下文，不覆盖目标活动上下文，也不向音箱播放、暂停或停止。设备组成员完全跳过同步。
- 切换路径不得触发 `resumeAfterReload` 自动起播；插件启动和热重载保留既有恢复行为。
- toggle 在目标正在播放时执行暂停；目标未播放且有有效待播放上下文时优先使用它。明确 resume 有有效待播放上下文时加载并起播，否则使用目标原上下文。
- 继续操作等待同一目标的异步同步任务。加载与起播分离：准备歌单、歌曲、位置、倍速、模式和 paused/stopped 状态时不生成 URL、不启动切歌定时器；准备成功后才下发，确认起播成功才提交新活动上下文并清除待播放上下文。
- 用户选择新歌单或新歌曲时清除待播放上下文，并使该目标未完成的同步任务失效。
- 快速连续切换按最新 revision 生效；旧任务不得覆盖新目标。同一目标内容未变化时去重且不刷新有效期，内容变化才更新。
- 加载、URL 生成或推流在下发前失败时保持目标不变；下发后失败或结果不确定时保留原本地记录和待播放上下文，沿用现有核验逻辑并报告失败或结果不确定，不自动重播旧歌曲。
- 歌曲恢复按 ID、索引、首曲回退；歌单不存在或为空时返回错误并保留目标原上下文。只有命中原 ID 才恢复位置，回退到其他歌曲从 0 秒开始，保留倍速和播放模式。
- 电台纳入快照但位置固定为 0；未知时长沿用快照位置，seek 精度由播放服务决定；接近曲尾遵循现有安全位置规则。上述情况按降级行为验收，不套用精确位置误差要求。
- 设备选择本身成功后，即使后台同步失败接口仍返回成功；同步错误由日志和后续继续操作报告。
- 普通语音和 AI 区分 pause、stop、resume；旧 AI 返回 `stop` 始终按停止处理，只有明确解析为 `pause` 才暂停。
- 快照写入按账号串行，并以单调 `updated_at` 或 revision 拒绝旧写入。删除账号时删除快照，新账号标识不继承；不迁移试做存储数据。

## 数据契约

`schema_version` 为整数，缺失或未知版本无效。`song_id` 是歌曲身份匹配依据，未命中时采用上文回退规则；标题和歌手仅用于诊断。`playlist_id` 必填，不能跨全库搜索歌曲。不读取试做版本的存储键或数据。

## 验收

纯逻辑测试使用独立内存存储和 mock 宿主 API，覆盖有效期、隔离、去重、revision、回退和状态；宿主集成测试使用真实宿主验证存储、歌单、设备状态和播放调用。集成验收须检查切换不发控制命令、不覆盖活动上下文，继续才发 URL，成功清除、失败保留，以及设备组跳过。夹具不得写入真实用户存储，当前阶段不添加业务测试。

真机验收覆盖独立设备、设备组、目标播放中切换、快速切换、弱网/离线/unknown/超时、歌单或歌曲变化、重启和过期、特殊歌曲及旧 AI `stop`。

## 文档职责

本文是唯一完整需求规范；issue 文件只记录任务拆分、依赖和验收入口；ADR-0002 记录架构取舍；ADR-0001 保留历史编号；CONTEXT.md 维护领域术语。`ready-for-agent` 表示需求可供实现，不表示代码或验收已完成。
